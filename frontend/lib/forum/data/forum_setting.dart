/// =======================================================
/// FORUM SETTING
/// - CHỈ QUẢN LÝ LUẬT DUYỆT
/// - KHÔNG CHỨA MODEL / STORE
/// =======================================================

class ForumSetting {
  /// true  = học sinh đăng → chờ duyệt
  /// false = học sinh đăng → hiện ngay
  static bool requireApproval = true;
}
